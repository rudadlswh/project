# Crossfit Android

- Open `android/` in Android Studio
- Update `BASE_URL` in `NetworkModule.kt` if needed
- Run on emulator or device

## Structure
- `ui/screens`: login/home/reserve/calendar/record/notice/profile
- `data/api`: Retrofit service + network module
- `data/repo`: repositories + token store
